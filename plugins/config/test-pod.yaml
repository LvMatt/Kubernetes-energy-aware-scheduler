apiVersion: v1
kind: Pod
metadata:
  name: test-pod-3
  namespace: kube-system
  "labels": {
    "app": "job"
  }
spec:
  schedulerName: energy-scheduler
  containers:
    - name: test-container
      image: nginx
